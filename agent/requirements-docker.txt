# Docker 运行时依赖（不含开发工具）
# 与 requirements.txt 保持同步；额外包含 Docker 专用依赖

# Web 框架
fastapi==0.111.0
uvicorn>=0.31.1
python-multipart>=0.0.6
httpx==0.27.1
aiohttp==3.9.5

# 配置 & 日志
pydantic>=2.11.0,<3.0.0
pydantic-settings>=2.5.2,<3.0.0
loguru==0.7.2
pyyaml==6.0.2
python-dotenv==1.0.1

# JWT
PyJWT>=2.10.1

# LLM 接入
openai==1.35.7
ollama==0.3.3

# 向量数据库 & 嵌入
# sentence-transformers 已移出：Docker 镜像用 ChromaDB 内置 ONNX embedding（all-MiniLM-L6-v2），
# 与 sentence-transformers 效果相同但无需安装 ~1GB PyTorch。代码已有自动降级逻辑。
chromadb==0.5.3
onnxruntime>=1.16.0

# 记忆 & 调度
croniter==2.0.5

# 工具
duckduckgo-search==6.2.13
psutil==6.0.0
requests>=2.31.0

# 数据库
PyMySQL>=1.1.3

# 监控
prometheus-client==0.20.0
nvidia-ml-py>=13.595.45

# MCP
mcp>=1.27.0
