# ============================================================
# Docker Compose 共享密钥配置
# 复制此文件为 .env，填入真实值后再运行 docker-compose up
# 注意: .env 文件不能提交到版本库
# ============================================================

# JWT 签名密钥（Java 后端签发 + Python Agent 验证，两侧必须一致）
# 生产环境请使用 openssl rand -base64 48 生成随机字符串
JWT_SECRET=your-strong-random-secret-here-minimum-32-characters

# 管理员账号密码（仅 Java 后端使用）
ADMIN_PASSWORD=your-admin-password-here

# CORS 允许来源（生产环境请设置为你的前端域名，多个用逗号分隔）
# 本地开发留空或保持 * 即可
# CORS_ALLOWED_ORIGINS=https://yourdomain.com

# ============================================================
# 可选 docker-compose 配置
# ============================================================

# Ollama 模型缓存目录：
#   留空使用 docker 命名卷（推荐，跨机器可移植）
#   或填入宿主机路径以共享下载好的模型，例如：
# OLLAMA_MODELS_DIR=D:/software/ollama
# OLLAMA_MODELS_DIR=/var/lib/ollama

# 是否在 Python Agent 镜像中安装 GitHub MCP（增加 ~200MB 镜像体积）
# 默认 0 不安装；如需开启：INSTALL_MCP=1 后 docker-compose build --no-cache agent
# INSTALL_MCP=1

# ComfyUI 安装目录（图片生成，--profile local 才会启动该容器）：
#   容器内部会复用此目录下的 models/checkpoints、custom_nodes，避免重复下载模型
# COMFYUI_DIR=D:/software/ComfyUI
# COMFYUI_DIR=/opt/ComfyUI

# ============================================================
# 本地非 Docker 开发时：
#   Java 后端: 在 IDE Run Configuration 或 shell 里设置上述两个环境变量
#              也可在 backend/web/ 下创建 .env 文件并配置 spring.config.import
#   Python Agent: 在 agent/.env 里设置 JWT_SECRET=... JWT_ENABLED=true
# ============================================================
